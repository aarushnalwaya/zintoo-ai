# ==========================================================
# OFFLINE TRAINING ONLY — never installed on the web server.
# Run these on Kaggle / Colab / a GPU box.
#   pip install -r requirements-train.txt
# ==========================================================
-r requirements.txt
torch>=2.1.0
torchvision>=0.16.0
pandas>=2.1.0
scikit-learn>=1.3.0
onnx>=1.15.0            # only needed by torch.onnx.export
tqdm>=4.66.0
